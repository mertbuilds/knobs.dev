services:
  postgres:
    image: postgres:17-alpine
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: knobs
      POSTGRES_PASSWORD: knobs
      POSTGRES_DB: knobs
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U knobs -d knobs']
      interval: 2s
      timeout: 3s
      retries: 15
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    profiles: [full]
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://knobs:knobs@postgres:5432/knobs
      NODE_ENV: production
    ports:
      - '3001:3001'
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'

volumes:
  pgdata:
