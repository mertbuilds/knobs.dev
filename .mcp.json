{
  "_comment": "Pin exact versions in args. Add per-product servers when accounts exist: posthog (npx -y @posthog/mcp@x.y.z, needs POSTHOG_API_KEY), sentry (npx -y @sentry/mcp-server@x.y.z, needs SENTRY_AUTH_TOKEN).",
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@4.0.4"]
    }
  }
}
