{
  "name": "@web-starter/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "postinstall": "prisma generate",
    "push": "node --env-file-if-exists=../../.env scripts/guarded-push.ts",
    "seed": "node --env-file-if-exists=../../.env seed.ts",
    "studio": "prisma studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.10.0",
    "@prisma/client": "^7.10.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/pg": "^8.15.4",
    "@web-starter/config": "workspace:*",
    "prisma": "^7.10.0",
    "typescript": "^7.0.2"
  }
}
